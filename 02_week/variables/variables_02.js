// Define a variable ageLimit that cannot be re-assigned and give it a value of 18.

const ageLimit = 18;
// if agelimit is assigned to any other number it gives an error as it is assigned to a constant variable

console.log(agelimit)
